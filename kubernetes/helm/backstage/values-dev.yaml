imagePullSecrets:
  - name: registry-credentials
fullnameOverride: backstage-dev
kubernetes:
  clusterName: local-cluster
  clusterUrl: https://192.168.1.200:6443
# Segredo criado manualmente no cluster (ver kubernetes/README.md), nunca commitado.
# Nenhuma credencial fica neste arquivo/Git.
secrets:
  existingSecret: backstage-dev-secrets
namespace:
  name: backstage-dev
global:
  baseUrl: http://developer-portal-dev.uday.corp
istio:
  enabled: true
  host: developer-portal-dev.uday.corp
postgres:
  auth:
    username: backstage
    password: backstage_dev_2024
    database: backstage_dev
  persistence:
    storageClassName: longhorn
backend:
  image:
    tag: v0.0.4
frontend:
  image:
    tag: v0.0.4
  nodeSelector:
    kubernetes.io/hostname: worker01
