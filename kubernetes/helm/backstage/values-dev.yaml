imagePullSecrets:
  - name: registry-credentials
fullnameOverride: backstage-dev

kubernetes:
  clusterName: local-cluster
  clusterUrl: https://192.168.1.200:6443

# Preencha os valores abaixo com as credenciais reais antes de aplicar.
# Token: kubectl create token backstage -n default --duration=8760h
# CA:    kubectl config view --raw --minify --flatten -o jsonpath='{.clusters[].cluster.certificate-authority-data}'
secrets:
  k8sServiceAccountToken: "SUBSTITUA_PELO_TOKEN"
  k8sCaData: "SUBSTITUA_PELO_CA_DATA"
  githubToken: "SUBSTITUA_PELO_GITHUB_TOKEN"
namespace:
  name: backstage-dev
global:
  baseUrl: https://developer-portal-dev.uday.corp
istio:
  enabled: true
  host: developer-portal-dev.uday.corp
postgres:
  auth:
    username: backstage
    password: backstage_dev_2024
    database: backstage_dev
  persistence:
    storageClassName: longhorn
backend:
  image:
    tag: v0.0.2
frontend:
  image:
    tag: v0.0.2
  nodeSelector:
    kubernetes.io/hostname: worker01
