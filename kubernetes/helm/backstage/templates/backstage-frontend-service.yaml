apiVersion: v1
kind: Service
metadata:
  name: {{ include "backstage.fullname" . }}-frontend
  namespace: {{ include "backstage.namespace" . }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "backstage.fullname" . }}-frontend
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
