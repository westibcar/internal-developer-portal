apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backstage-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: backstage-prod
    app.kubernetes.io/part-of: backstage
    environment: prod
spec:
  project: backstage
  source:
    repoURL: https://github.com/westibcar/internal-developer-portal.git
    targetRevision: main
    path: kubernetes/helm/backstage
    helm:
      releaseName: backstage-prod
      valueFiles:
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: backstage
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      jqPathExpressions:
        - .spec.volumeClaimTemplates[].status
        - .spec.volumeClaimTemplates[].apiVersion
        - .spec.volumeClaimTemplates[].kind
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
